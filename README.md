# AI-Quiz-App

To run Frontend

Use Command - npm run dev

To run backend

Use Command - uv run server.py

To add dependencies 

Use command - uv add name of dependency

To change the no. of quotas- 
change the qutos in model.py and db.py
